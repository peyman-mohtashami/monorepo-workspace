import React, { Component } from "react";
import { View, Text } from "react-native";

class CustomButton extends Component {
    state = {};
    render() {
        return (
            <View>
                <Text>Custom Button Native</Text>
            </View>
        );
    }
}

export default CustomButton;
